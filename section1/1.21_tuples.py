monday_temps = (1, 3, 6)
#this is a tuple. 
# a tuple is just like a list but has parenthesis instead of brackets.
# another difference is that tuples are immutable, whereas lists are mutable (can be changed)

#tuples are faster than lists 